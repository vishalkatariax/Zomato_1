{
  "buildCommand": "echo 'Static frontend — no build step'",
  "installCommand": "echo 'Static frontend — no install step'",
  "framework": null,
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://<YOUR-RENDER-APP-NAME>.onrender.com/api/:path*"
    },
    {
      "source": "/health",
      "destination": "https://<YOUR-RENDER-APP-NAME>.onrender.com/health"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
