services:
  - type: web
    name: restaurant-recommendation-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir src restaurant_app:app
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true
